# Dove Zip — UI/UX 설계 (v0.1)

[PLAN.md](PLAN.md)(기능/MVP), [ARCHITECTURE.md](ARCHITECTURE.md)(구조)를 기반으로 한
UI/UX 초안. 비주얼 톤은 같은 제작자의
[daylight-commander-flutter](../daylight-commander-flutter) 테마를 **색상 값 변경
없이 그대로 이식**한다. 다른 점은 화면 구성뿐이다 — daylight는 2-pane 파일 탐색기고,
Dove Zip은 **단일 뷰 압축파일 탐색기 + 생성 화면**이다.

## 1. 디자인 원칙

- **편의성이 최우선 설계 목표다**: PLAN.md에서 "가장 중요한 기능은 편리함"이라고
  명시했듯, 이 앱의 UI는 다른 무엇보다 "압축 해제 위치를 매번 고민하지 않게" 만드는
  것을 최우선으로 한다 — 3가지 해제 모드 버튼(7장 `ExtractModeBar`)이 화면에서
  가장 눈에 띄는 요소여야 한다.
- **아이콘이 정보다** (daylight 원칙 계승): 포맷을 텍스트보다 컬러 아이콘으로 먼저
  인지하게 한다. 여기에 더해 **"생성 가능/해제 전용" 여부를 배지로 항상 표시**해
  RAR처럼 해제만 되는 포맷에서 사용자가 헷갈리지 않게 한다(3장).
- **라이트/다크 동등**: daylight와 동일하게 한쪽만 신경 쓴 티가 나지 않게 한다.
- **한 화면, 두 흐름**: 압축파일을 "여는" 흐름과 새로 "만드는" 흐름이 하나의
  창 안에서 자연스럽게 전환된다 — daylight처럼 좌우 패널을 나누지 않고, 대신
  빈 상태/열림 상태 두 가지 상태를 오간다(6장).

## 2. 컬러 시스템 (daylight 팔레트 그대로 이식)

daylight의 `AppColors`/`AppRadius`/`AppTheme.dark()/.light()`를 값 변경 없이
그대로 복사해 온다 (`lib/presentation/theme/app_theme.dart` 파일 자체를 이식).

| 역할 | Dark | Light |
|---|---|---|
| Background | `#0A0E14` | `#F4F6FA` |
| Surface (카드/리스트) | `#151D27` | `#FFFFFF` |
| Surface 강조 | `#1D2733` | `#E3E8EF` |
| Primary (브랜드, "알아서" 기본 액션) | `#4C9DFF` | `#2C6BE0` |
| Accent (보조 강조) | `#7C5CFF` | `#7C5CFF` |
| Success (생성+해제 가능 배지) | `#34D399` | `#34D399` |
| Warning (해제 전용 배지 — RAR 등) | `#FFB020` | `#FFB020` |
| Danger (실패/삭제) | `#FF5A5F` | `#FF5A5F` |

**신규 의미 부여** (daylight에는 없던 이 앱만의 색상 용도):
- `Success`(`#34D399`) = 압축 **생성+해제** 모두 지원하는 포맷의 배지 색
- `Warning`(`#FFB020`) = **해제 전용** 포맷(RAR, ISO, CAB 등)의 배지 색 — 경고가
  아니라 "이 포맷은 못 만듭니다"를 정직하게 알리는 정보성 표시. 압축 생성 시
  포맷 드롭다운에서는 아예 선택지에서 제외되므로(4장), 이 배지는 **압축파일을
  열었을 때**만 보인다.

## 3. 압축 포맷 아이콘 & 배지 시스템

> daylight에 이미 압축 관련 컬러 SVG 아이콘이 준비돼 있어(icons8 세트) 그대로
> 가져다 쓸 수 있다: `icons8-zip-240`, `icons8-7zip-240`, `icons8-rar-240`,
> `icons8-tar-240`, `icons8-archive-240`, `icons8-open-archive-240`.

| 포맷 | 아이콘 | 배지 |
|---|---|---|
| ZIP | `icons8-zip-240.svg` | 생성+해제 (Success) |
| 7Z | `icons8-7zip-240.svg` | 생성+해제 (Success) |
| TAR / TAR.GZ / TAR.BZ2 / TAR.XZ / TAR.ZST | `icons8-tar-240.svg` | 생성+해제 (Success) |
| GZIP / BZIP2 / XZ / ZSTD (단일 파일) | `icons8-archive-240.svg` (폴백) | 생성+해제 (Success) |
| **RAR** | `icons8-rar-240.svg` | **해제 전용 (Warning)** |
| ISO/CAB/ARJ/LHA/CPIO/AR/XAR/WARC | `icons8-archive-240.svg` (폴백) | 해제 전용 (Warning) |
| 매칭 없는 압축 형식 | `icons8-open-archive-240.svg` (범용 폴백) | 상황에 따라 |

- 압축파일 안 개별 파일 항목은 daylight의 파일 유형 아이콘 세트(jpg/png/pdf/mp3
  등)를 **그대로 재사용** — 자매 앱이라 `assets/icons/filetypes/`를 통째로 공유해도
  무방
- **배지 렌더링**: 아이콘 우측 하단에 8×8 원형 점(Success/Warning) — 목록에서
  스캔하기 쉽도록 텍스트 라벨 없이 색으로만 구분하고, 툴팁에 "이 형식은 압축
  생성을 지원하지 않습니다(해제만 가능)" 등 설명 표시(접근성 10장과 연계)

## 4. 아이콘 시스템 (패키지 선택)

daylight와 동일한 결론 재사용: **UI 크롬(툴바/메뉴/다이얼로그)은 Material
Icons**, **포맷/파일 아이콘은 `flutter_svg` + icons8 컬러 SVG**
(`phosphor_flutter`가 최신 Flutter SDK의 `IconData final class` 제약으로 컴파일이
깨지는 문제를 daylight에서 이미 확인했기 때문 — 재조사 불필요).

**이 앱에서 새로 필요한 아이콘** (daylight에 없던 것, icons8에서 동일 스타일로
추가 수급 필요):
- 3가지 해제 모드 버튼용: "여기에"(폴더+화살표 아래), "알아서"(마법봉/자동 아이콘),
  "원하는 곳에"(폴더 선택/찾아보기 아이콘)
- 비밀번호 잠금(압축 생성 시 암호 옵션)
- 압축 레벨 게이지(저장/빠름/보통/최대)
- 분할 볼륨(레이어/조각 아이콘)
- 빈 상태 드롭존 일러스트(daylight의 "빈 상태 일러스트 48"과 같은 사이즈 규칙 계승)

사이즈 스케일은 daylight와 동일하게: 리스트 행 22 / 툴바 액션 18 / 빈 상태
일러스트 48. `ExtractModeBar`의 3버튼 아이콘만 예외적으로 24(주 액션이라 약간
키움).

## 5. 타이포그래피

daylight와 완전히 동일한 정책을 이식한다:
- UI 크롬: 커스텀 폰트 **SeoulNamsan** (Light/Regular/Bold/ExtraBold) 재사용
- 압축파일/항목 이름: 시스템 기본 폰트(임의 유니코드 파일명 커버리지 확보 목적)
- 크기: 리스트 행 13 / 컬럼 헤더 11.5(medium) / 상태바 11 / 다이얼로그 제목
  16(bold) — 단, `ExtractModeBar`의 버튼 라벨은 14(semibold)로 daylight 기준보다
  한 단계 키워 시각적 우선순위를 확보(1장 "편의성 최우선" 원칙 반영)

## 6. 레이아웃 구조

### 6.1 빈 상태 (압축파일 없음, 앱 시작 시 기본 화면)

```
┌─ 타이틀바 (커스텀, window_manager) ────────────────────────┐
├─ 툴바: [열기] [새 압축 만들기]           [테마][언어] ───────┤
├────────────────────────────────────────────────────────┤
│                                                          │
│              (드롭존 일러스트, 48pt)                       │
│        "압축파일을 여기로 드래그하거나 클릭해서 열기"           │
│                                                          │
│              [ 새 압축 만들기 ]  ← Primary 버튼            │
│                                                          │
│              (최근 연 압축파일 목록, 있으면 아래에 몇 개)      │
└──────────────────────────────────────────────────────────┘
```

### 6.2 열림 상태 (압축파일 탐색 중)

```
┌─ 타이틀바 ─────────────────────────────────────────────────┐
├─ 툴바: [뒤로][닫기] photos.tar.gz [배지: 생성+해제]  [검색][테마][언어] ┤
├────────────────────────────────────────────────────────────┤
│  ┌ 컬럼헤더: 이름 / 크기 / 압축크기 / 수정일 ───────────────┐   │
│  ├─────────────────────────────────────────────────────┤   │
│  │ [칩]📁 docs/                                          │   │
│  │ [칩]🖼 photo.jpg           2.1MB  1.9MB  2026-01-03   │   │
│  │      ...                                              │   │
│  └─────────────────────────────────────────────────────┘   │
├────────────────────────────────────────────────────────────┤
│  ExtractModeBar (항상 보임, 7장):                            │
│  [ 여기에 압축 해제 ]  [ ▶ 알아서 압축 해제 (기본) ]  [ 원하는 곳에... ] │
├────────────────────────────────────────────────────────────┤
│  상태바: 항목 N개·전체 크기 | 진행률 pill | 포맷 배지            │
└────────────────────────────────────────────────────────────┘
```

### 6.3 압축 생성 흐름 (파일/폴더 드롭 또는 "새 압축 만들기")

압축파일을 여는 화면과 별개의 다이얼로그(`CompressDialog`)로 처리한다 —
daylight의 `ConnectionDialog`처럼 모달로 띄운다.

```
┌─ 새 압축 만들기 ───────────────────────────┐
│  대상: photo.jpg, report.pdf, docs/ (3개)  │
│  포맷: [ZIP ▾]  (해제 전용 포맷은 목록에서 자동 제외) │
│  압축 레벨: 저장 ─●───── 최대                │
│  □ 비밀번호로 보호  [________]              │
│  □ 분할 압축 (볼륨 크기: [___] MB)          │
│  저장 위치: ~/Downloads/archive.zip [변경]  │
│                                            │
│              [취소]        [압축 시작]       │
└────────────────────────────────────────────┘
```

## 7. 핵심 컴포넌트

- **`ExtractModeBar`** (이 앱의 대표 컴포넌트): 열림 상태 화면 하단에 항상
  고정으로 보이는 3버튼 그룹.
  - "여기에 압축 해제": Outlined 스타일
  - "알아서 압축 해제": **Filled(Primary) + 기본 강조** — PLAN.md가 정한 기본값을
    시각적으로도 기본값으로 보이게 함(3개를 동급으로 노출하되 기본은 눈에 띄게)
  - "원하는 곳에 압축 해제...": Outlined 스타일, 클릭 시 OS 폴더 선택 다이얼로그
  - 셋 다 클릭 즉시 실행(추가 확인 없음) — "여기에"/"알아서"는 대상 폴더가
    이미 확정돼 있으므로 다이얼로그 없이 바로 진행률 배너가 뜬다(ARCHITECTURE.md
    5장 `resolveExtractDestination`과 1:1 대응)
  - 선택 항목이 있으면 버튼 라벨이 "선택 항목 해제"로 바뀜(전체 대상 vs 부분
    대상 구분), 없으면 전체 해제 대상
- **`FormatBadge`**: 3장의 생성+해제(Success)/해제 전용(Warning) 배지 — 아이콘
  칩 우측 하단에 렌더링, 압축파일 자체(툴바의 파일명 옆)와 포맷 선택
  드롭다운(압축 생성 시) 양쪽에서 재사용
- **`ArchiveEntryRow`**: daylight의 `FileRow`를 거의 그대로 이식 — 아이콘 칩 +
  이름(말줄임) + 크기 + 압축 크기(daylight에는 없던 컬럼) + 수정일. 선택 시
  `Primary` 10~14% 알파 배경, 키보드 커서는 별도 테두리(daylight와 동일 모델)
- **`ConflictDialog`**: daylight 컴포넌트 그대로 재사용(덮어쓰기/건너뛰기/
  이름변경/모두 적용) — 해제 대상 폴더에 기존 파일이 있을 때
- **`PasswordPromptDialog`**: 암호로 보호된 zip/7z를 열거나 해제할 때 표시,
  "이번 세션 동안 기억" 체크박스(저장은 안 하지만 같은 압축파일 재조작 시 재입력
  방지 — ARCHITECTURE.md 10장 "비밀번호 저장 안 함" 원칙과 일치)
- **`CompressDialog`**: 6.3의 압축 생성 폼. 포맷 드롭다운은 `FormatRegistry`의
  `canWrite == true`인 포맷만 노출(RAR 등은 애초에 선택지에 없음)
- **`OperationBanner`**: daylight의 진행률 카드를 그대로 이식 — 압축/해제
  진행률 바 + 취소(X) 버튼. 파일 단위 취소 원칙도 동일(ARCHITECTURE.md 6장)
- **드래그앤드롭**: 압축파일을 창에 드래그하면 열림 상태로 전환(더블클릭과 동일
  동작), 일반 파일/폴더를 드래그하면 `CompressDialog`가 드롭된 항목을 미리
  채운 채로 열림 — **여러 압축파일을 동시에 드롭하면 각각을 같은 모드로 일괄
  해제**(P1, PLAN.md 1.2 편의 기능의 자연스러운 확장)

## 8. 인터랙션 & 단축키

| 키 | 동작 |
|---|---|
| Ctrl/Cmd+O | 압축파일 열기 |
| Ctrl/Cmd+N | 새 압축 만들기 |
| ↑ / ↓ | 커서 이동 (daylight와 동일 — 마우스 클릭과 다중선택 모델 분리) |
| Space | 커서 항목 다중선택 토글 |
| Enter | 폴더면 진입, 파일이면 미리보기(F3와 동일 동작) |
| Backspace | 상위 폴더로 |
| F3 | 미리보기 |
| Ctrl/Cmd+E | **빠른 압축 해제** — 현재 기본 모드("알아서")를 즉시 실행,
  `ExtractModeBar`를 마우스로 누르지 않고도 가장 흔한 동작을 바로 실행 |
| Ctrl/Cmd+Shift+E | 압축 해제 모드 선택에 포커스 이동(3버튼 중 키보드로 고르기) |
| Ctrl/Cmd+A | 전체 선택 |
| Ctrl/Cmd+F | 압축파일 내부 검색(퀵서치, daylight 계승) |
| `/` 또는 타이핑 시작 | 퀵서치(daylight와 동일) |

> **구현 현황 (PLAN.md 1.2 "선택 항목만 해제")**: 이 표는 daylight와 똑같이
> "↑/↓ 커서 이동 + Space 토글"이라는 키보드 전용 다중선택 모델을 목표로
> 적었지만, 실제로는 그 자리를 **Ctrl(⌘)+클릭(하나씩 토글)/Shift+클릭(범위
> 선택)/Ctrl(⌘)+A(전체 선택)**로 대신 구현했다 — 이 앱은 아직 다른
> 화면에서도 Ctrl/Cmd+O, F3, Backspace 같은 나머지 단축키 인프라 자체가
> 없어서, 화살표 키 커서 이동만 이번 기능을 위해 먼저 만들면 오히려
> 일관성이 떨어진다고 판단했다. 마우스 클릭(일반 클릭)은 여전히 선택에
> 관여하지 않고 폴더 진입/미리보기만 한다는 원칙은 그대로 지켰다. ↑/↓
> 커서와 Space 토글, 그리고 이 표의 나머지 단축키들은 앱 전체 키보드
> 단축키를 한 번에 붙이는 별도 작업으로 남겨둔다.

**드래그앤드롭 방향 제약**: daylight가 "앱 → OS 드래그(내보내기)"를 난이도 대비
실익이 낮다고 판단해 비목표로 확정한 것과 동일한 이유로, Dove Zip도 **압축파일
내부 항목을 Finder/탐색기로 직접 드래그해서 꺼내는 기능은 P2/비목표 후보**로
둔다 — 대신 `ExtractModeBar`의 "여기에"/"원하는 곳에"가 사실상 그 역할을 더 빠르고
명확하게 대체한다.

> **구현 현황 (2026-09-24, PLAN.md 1.2 "끌어내서 해제")**: 이후 자체 플러그인
> `flutter_drag_out`(Flutter `Draggable`을 그대로 두고 포인터가 창을 벗어나는
> 순간에만 OS 드래그로 넘김)이 생기면서 난이도 문제가 풀려, 1단계를 구현했다.
> 압축 목록의 행을 끌면 드래그 미리보기(항목 이름, 여러 개면 "N개 항목")에
> 준비 상태가 함께 보인다 — 푸는 중이면 작은 스피너, 비밀번호가 필요하면
> 자물쇠, 너무 크거나 실패하면 금지 아이콘. 창 안에서 놓으면 아무 일도
> 일어나지 않는다(창 안에 드롭 대상 없음). 창 밖까지 끌었는데 넘기지
> 못했으면 이유를 스낵바로 알린다. `ExtractModeBar`가 여전히 기본 경로이고,
> 끌어내기는 작은 항목을 원하는 폴더에 바로 떨구는 보조 수단이다.

## 9. 반응형/윈도우 크기

- 최소 윈도우 크기: 720×480 (단일 뷰라 daylight의 2-pane 960×600보다 작아도 됨)
- 압축 생성 다이얼로그(`CompressDialog`)는 고정 폭 480, 내용에 따라 높이만 가변
- 데스크톱 전용이므로 그 이하 반응형 대응은 불필요

## 10. 접근성

daylight와 동일한 기준 유지:
- 텍스트 대비 WCAG AA 이상(동일 팔레트이므로 값 재검증 불필요)
- 모든 상호작용 요소에 `Primary` 2px 포커스 링
- 아이콘 전용 버튼은 툴팁 + 스크린리더 라벨
- **추가**: `FormatBadge`(3장)는 색상만으로 구분되므로 반드시 툴팁/스크린리더
  라벨("생성 및 해제 가능" / "해제만 가능")을 병행 — 색맹 사용자가 배지 색만으로
  RAR이 해제 전용인지 구분 못 하는 상황 방지

## 다음 단계
PLAN.md 7장 / ARCHITECTURE.md "다음 단계"와 동일 — 이 문서로 화면 설계는 일단
마무리하고, ARCHITECTURE.md 6.4의 네이티브 백엔드 PoC(Windows libarchive 빌드
등)를 먼저 검증한 뒤 프로젝트 스캐폴딩 및 P0 구현으로 넘어간다. 3가지 해제 모드
버튼(`ExtractModeBar`)은 네이티브 백엔드 없이도(Dart `archive` 패키지의 zip만으로)
가장 먼저 눈으로 확인 가능한 화면이므로, 스캐폴딩 직후 가장 먼저 구현해 볼
후보로 제안한다.
